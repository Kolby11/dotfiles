# 📦 Dotfiles

My personal configuration files managed with **GNU Stow**. This setup allows for a modular, version-controlled environment that can be deployed with a single command.

## 🚀 Activation

To symlink all configurations to your home directory, run the following command from the root of this repository:

```bash
stow -vt ~ */


# Fastfetch themes
src - https://github.com/LierB/fastfetch